appname=Redis
appdescription=Redis is an open source, BSD licensed, advanced key-value store. It is often referred to as a data structure server since keys can contain strings, hashes, lists, sets and sorted sets.
appurl=redis.io
appprice=100
defaultlogin=root
defaultpassword=rooter
appfeature1=* Atomic operations like appending to a string, incrementing the value in a hash, pushing to a list, computing set intersection, union and difference, getting the member with highest ranking in a sorted set
appfeature2=* In-memory dataset by dumping the dataset to disk, by appending each command to a log
appfeature3=* Master-slave replication with very fast non-blocking first synchronization, auto-reconnection on net split 
appfeature4=* Transactions
appfeature5=* Pub/sub
appfeature6=* Lua scripting 
appfeature7=* Keys with a limited time-to-live
appfeature8=* Configuration settings
appfeature9=
appfeature10=
